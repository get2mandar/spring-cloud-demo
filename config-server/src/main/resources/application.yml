spring:
  application:
    name: config-server
  profiles:
      active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/config/{application}
server:
  port: 8888
logging:
  file: logs\config-server.log
  level:
    com.example.scdemo.infra.configserver: DEBUG